# Web UI (Route B) — generated assets; run `ui/npm run build` before Release
ui/node_modules/
ui/.vite/
src/ArrayMicRefreshment.App/wwwroot/

# Build outputs
bin/
obj/
[Bb]uild/
[Ll]og/
[Ll]ogs/
TestResults/
coverage/
*.trx
*.coverage
*.coveragexml

# Visual Studio / Rider / VS Code
.vs/
.vscode/
.idea/
*.user
*.suo
*.userosscache
*.sln.docstates
*.rsuser

# NuGet
packages/
*.nupkg
project.lock.json
project.fragment.lock.json
artifacts/

# Downloaded ONNX models (large; use scripts/download-models.ps1)
models/

# Local release / publish outputs (do not commit)
dist/
publish_output/
publish-test/
*.zip

# CER measurement audio (generated locally)
scripts/cer-audio/

# App data / logs (local dev)
*.log

# OS
.DS_Store
Thumbs.db
Desktop.ini

# Cursor / agent — track shared rules; ignore local cache
.cursor/*
!.cursor/rules/
!.cursor/rules/**
agent-transcripts/
