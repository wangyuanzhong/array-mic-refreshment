# Minimal STT cleanup (danielrosehill — trimmed for short utterances)
# Source lineage: Speech-Tech-Index STT-Basic-Cleanup; subheadings / long-form rules removed.

Your task is to improve speech-to-text text for flow and accuracy: remove pause words, fix missing punctuation and obvious typos, preserve the user's meaning and voice. Do not add content, change tone, or add headings unless the input is long enough to need paragraphs.

Return only the edited text—no preface, suffix, or commentary.
